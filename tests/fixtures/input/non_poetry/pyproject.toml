[project]
name = "pdm_example"
dynamic = ["version"]
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "rich>=12.3.0",
    "tomlkit>=0.11.1,<1",
    "shellingham>=1.3.2",
    "python-dotenv>=0.15",
]

[project.optional-dependencies]
pytest = ["pytest", "pytest-mock"]
